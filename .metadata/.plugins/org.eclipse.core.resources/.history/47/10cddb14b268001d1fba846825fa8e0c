package semi.kiten.service;

import java.util.List;

import semi.kiten.domain.KitenVO;



public interface KitenService {
	// CRUD 기능의 메소드 구현
	// 글 등록
	void insertBoard(KitenVO vo);

	// 글 수정
	void updateBoard(KitenVO vo);

	// 글 삭제
	void deleteBoard(KitenVO vo);

	// 글 상세 조회
	KitenVO getBoard(KitenVO vo);

	// 글 목록 조회
	List<KitenVO> getBoardList(KitenVO vo);
}
